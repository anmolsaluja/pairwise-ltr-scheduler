# NOTE: Do NOT list torch here.
# On Google Colab, `pip install torch` from PyPI installs a CPU-only build and
# breaks CUDA. Colab already ships a CUDA torch — leave it alone.
#
# Local Mac/Linux: install torch yourself first, then:
#   pip install -r requirements.txt

transformers>=4.43.0
datasets>=2.14.0
accelerate>=0.27.0
bitsandbytes>=0.43.0; platform_system != "Darwin"
huggingface_hub>=0.23.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Live serving (GPU only — install separately, not on CPU Macs):
#   pip install vllm
# vllm>=0.8.0
